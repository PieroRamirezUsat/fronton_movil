<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite HTTP sin cifrar SOLO en builds de debug, y solo hacia el servidor local
  de desarrollo (127.0.0.1 vía adb reverse, y 10.0.2.2 si algún día se recupera el
  acceso directo del emulador sin pasar por adb). Este archivo vive en
  src/debug/, por lo que NUNCA se empaqueta en un build de release — la app en
  producción sigue exigiendo HTTPS por el comportamiento por defecto de Android.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
